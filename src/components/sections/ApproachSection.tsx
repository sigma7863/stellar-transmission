export default function ApproachSection() {
  return (
    <section className="content-section section-shell" data-section="approach">
      <div className="content-section__intro" data-reveal>
        <p className="section-kicker">Approach Vector</p>
        <h2>Built as a forward motion system, not a static landing page.</h2>
      </div>
      <div className="approach-layout">
        <p data-reveal>
          The visual field stays sparse on purpose. Each layer exists to suggest
          distance, discipline, and instrument-grade feedback rather than generic
          motion for its own sake.
        </p>
        <p data-reveal>
          Scroll movement tightens the composition, cards respond like calibrated
          modules, and the background scene behaves like a navigational frame
          rather than a decorative wallpaper.
        </p>
      </div>
    </section>
  );
}
